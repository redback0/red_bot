import random

async def use(qty):
	total = 0
	for i in range(qty):
		total += _amount()




def _amount():

	# gets a random amount between 1 and 50
	p1 = random.randrange(1, 22.4, 0.1)
	p2 = random.randrange(1, 22.4, 0.1)

	return (p1 * p2 / 100) # a number between 1 and 3.7^3
